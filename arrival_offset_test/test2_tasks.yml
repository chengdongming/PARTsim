# 新测试场景：不���的offset和周期
taskset:
  - name: task_A
    iat: 400
    runtime: 100
    deadline: 400
    params: "period=400,wcet=100,arrival_offset=0,workload=bzip2"
    code:
      - fixed(100, bzip2)

  - name: task_B
    iat: 600
    runtime: 150
    deadline: 600
    params: "period=600,wcet=150,arrival_offset=150,workload=bzip2"
    code:
      - fixed(150, bzip2)

  - name: task_C
    iat: 800
    runtime: 200
    deadline: 800
    params: "period=800,wcet=200,arrival_offset=300,workload=hash"
    code:
      - fixed(200, hash)
