# Formal real-trace inputs are intentionally not frozen. Replace the fixture only
# after source, license, units, preprocessing, and publication rights are audited.
experiment_id: asap-block-v9.3-ext2-template-workload-contract-v2
core: CORE-3
extension: EXT-2
platform: {cores: [4], task_count: [10]}
generation:
  deadline_mode: implicit
  constrained_deadline: {d_over_t_values: [], d_over_t_min: "0", d_over_t_max: "1", distribution: generator_uniform_integer}
  period_min: 40
  period_max: 200
  wcet_rounding: compensated
  utilization_tolerance: "0.01"
  min_task_util: "0.01"
  max_task_util: "0.8"
  priority_policy: RM
  power_mode: generator_default_heterogeneous
  workload_candidates: [bzip2, control, decrypt, encrypt, hash]
  generator_timeout_seconds: 120
energy:
  initial_energy_values: ["1"]
  simulation_initial_battery: "20"
  exact_rational_encoding: canonical_fraction
  service_curve: {id: configure-before-formal-run, system_template: system_config_unified_template.yml, horizon: 30000}
  battery_mode: finite
  battery_capacity: "100"
grid: {utilization_points: ["0.2"], tasksets_per_cell: 1, base_seed: 930620, seed_mode: generation_dimensions}
rta: {methods: [CW_THETA_CW, LOC_THETA_LOC], timeout_seconds: 10, retry_timeout_seconds: 20, retry_policy: timeout_once, numerical_mode: EXACT_RATIONAL}
simulation: {horizon: 1000, warmup: 0, minimum_jobs_per_task: 3, maximum_horizon: 2000, horizon_extension_policy: double, deadline_miss_fail_fast: true, timeout_seconds: 60, trace_mode: semantic, trace_on_failure: true, simulator_bin: ./build/rtsim/rtsim}
real_trace:
  data_status: REAL_TRACE_DATA_UNAVAILABLE
  input_file: test/fixtures/v9_3_ext2_synthetic_trace.csv
  schedulers: [gpfp_asap_block]
  scale: "1"
  schema: {trace_id: replace-after-audit, source_id: unavailable, timestamp_column: timestamp, timestamp_format: iso8601, duration_column: duration_seconds, value_column: power_w, quantity_kind: INSTANTANEOUS_POWER, unit: W, missing_policy: reject, preprocessing_version: UNAVAILABLE, fixture_label: SYNTHETIC_TEST_FIXTURE}
  segment_selection: {rule: FIRST_N_SAMPLES_PREDECLARED, start_sample: 0, sample_count: 3}
  resampling: {target_interval_seconds: "60", policy: exact_subdivision, declared_interpolation: piecewise_constant}
  service_bound: {enabled: false, applicable_horizon_intervals: 0}
execution: {worker_count: 1, checkpoint_every: 1, output_root: artifacts/v9_3_ext2_template_workload_contract_v2, taskset_store: artifacts/v9_3_ext2_taskset_store_workload_contract_v2, resume: false, fail_fast_on_p0: true, preserve_attempt_history: true}
plots: {}
