experiment_id: asap-block-v9.3-core4-formal-sustainability-r1-workload-contract-v2
core: CORE-4
parameter_status: FROZEN_FOR_FORMAL_EXECUTION
platform:
  cores: [4]
  task_count: [10]
generation:
  deadline_mode: constrained
  constrained_deadline:
    d_over_t_values: []
    d_over_t_min: "0"
    d_over_t_max: "1"
    distribution: generator_uniform_integer
  period_min: 40
  period_max: 200
  wcet_rounding: compensated
  utilization_tolerance: "1/100"
  min_task_util: "1/100"
  max_task_util: "4/5"
  priority_policy: RM
  power_mode: generator_default_heterogeneous
  workload_candidates: [bzip2, control, decrypt, encrypt, hash]
  generator_timeout_seconds: 120
energy:
  initial_energy_values: ["1/20"]
  exact_rational_encoding: canonical_fraction
  service_curve:
    id: paired-real-solar-service-scale-1-v1
    system_template: system_config_unified_template.yml
    horizon: 30000
    exact_scale: "1"
  battery_mode: finite
  battery_capacity: "20"
grid:
  utilization_points: ["3/10", "2/5", "1/2", "3/5", "7/10"]
  tasksets_per_cell: 200
  base_seed: 930444
  seed_mode: generation_dimensions
  taskset_index_start: 0
analysis:
  variants: [CW_THETA_CW, LOC_THETA_LOC]
  timeout_seconds: 60
  retry_timeout_seconds: 120
  retry_policy: timeout_once
  worker_count: 4
  numerical_mode: EXACT_RATIONAL
sensitivity:
  profile: formal-sustainability-v1
  baseline:
    E0: "1/20"
    power_scale: "1"
    service_curve_scale: "1"
    battery_capacity: "20"
  axes:
    initial_energy:
      values: ["0", "1/100", "1/50", "3/100", "1/20", "1/5", "1"]
    service_curve:
      variants:
        - {id: paired-real-solar-service-scale-1-2-v1, system_template: system_config_unified_template.yml, horizon: 30000, exact_scale: "1/2", availability: AVAILABLE}
        - {id: paired-real-solar-service-scale-3-4-v1, system_template: system_config_unified_template.yml, horizon: 30000, exact_scale: "3/4", availability: AVAILABLE}
        - {id: paired-real-solar-service-scale-1-v1, system_template: system_config_unified_template.yml, horizon: 30000, exact_scale: "1", availability: AVAILABLE}
        - {id: paired-real-solar-service-scale-5-4-v1, system_template: system_config_unified_template.yml, horizon: 30000, exact_scale: "5/4", availability: AVAILABLE}
        - {id: paired-real-solar-service-scale-3-2-v1, system_template: system_config_unified_template.yml, horizon: 30000, exact_scale: "3/2", availability: AVAILABLE}
    power_scale:
      values: ["1/2", "3/4", "1", "5/4", "3/2"]
    method:
      variants: [CW_THETA_CW, LOC_THETA_LOC]
execution:
  checkpoint_every: 1
  output_root: results/v9_3_core4_formal_sustainability_r1_workload_contract_v2
  taskset_store: results/v9_3_core4_taskset_store_formal_r1_workload_contract_v2
  resume: false
  fail_fast_on_p0: true
  preserve_attempt_history: true
plots: {}
