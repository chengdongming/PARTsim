# =============================================
# ASAP能量优先级调度测试任务集
# 目标:验证能量预算检查是否正确阻止低优先级任务
# =============================================

taskset:
  # 高优先级任务0 (bzip2, 0.1J/50ms)
  - name: task_high_prio_0
    iat: 1000
    runtime: 200
    deadline: 500
    params: "period=1000,arrival_offset=0,workload=bzip2"
    code:
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)

  # 高优先级任务1 (bzip2, 0.1J/50ms)
  - name: task_high_prio_1
    iat: 1200
    runtime: 200
    deadline: 600
    params: "period=1200,arrival_offset=0,workload=bzip2"
    code:
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)

  # 中优先级任务2 (hash, 0.08J/50ms)
  - name: task_mid_prio_2
    iat: 1400
    runtime: 200
    deadline: 800
    params: "period=1400,arrival_offset=0,workload=hash"
    code:
      - fixed(50, hash)
      - suspend(0)
      - fixed(50, hash)
      - suspend(0)
      - fixed(50, hash)
      - suspend(0)
      - fixed(50, hash)
      - suspend(0)

  # 中优先级任务3 (hash, 0.08J/50ms)
  - name: task_mid_prio_3
    iat: 1600
    runtime: 200
    deadline: 1000
    params: "period=1600,arrival_offset=0,workload=hash"
    code:
      - fixed(50, hash)
      - suspend(0)
      - fixed(50, hash)
      - suspend(0)
      - fixed(50, hash)
      - suspend(0)
      - fixed(50, hash)
      - suspend(0)

  # 低优先级任务4 (bzip2, 0.1J/50ms)
  - name: task_low_prio_4
    iat: 1800
    runtime: 200
    deadline: 1200
    params: "period=1800,arrival_offset=0,workload=bzip2"
    code:
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)

  # 低优先级任务5 (bzip2, 0.1J/50ms)
  - name: task_low_prio_5
    iat: 2000
    runtime: 200
    deadline: 1400
    params: "period=2000,arrival_offset=0,workload=bzip2"
    code:
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)
      - fixed(50, bzip2)
      - suspend(0)
