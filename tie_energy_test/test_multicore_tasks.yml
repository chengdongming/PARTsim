taskset:
  - name: task1_bzip2
    iat: 100
    runtime: 20
    deadline: 100
    params: "period=100,wcet=20,arrival_offset=0,workload=bzip2"
    code:
      - fixed(20, bzip2)

  - name: task2_bzip2
    iat: 100
    runtime: 20
    deadline: 100
    params: "period=100,wcet=20,arrival_offset=0,workload=bzip2"
    code:
      - fixed(20, bzip2)

  - name: task3_hash
    iat: 150
    runtime: 15
    deadline: 150
    params: "period=150,wcet=15,arrival_offset=0,workload=hash"
    code:
      - fixed(15, hash)

  - name: task4_control
    iat: 200
    runtime: 10
    deadline: 200
    params: "period=200,wcet=10,arrival_offset=0,workload=control"
    code:
      - fixed(10, control)

  - name: task5_control
    iat: 200
    runtime: 5
    deadline: 200
    params: "period=200,wcet=5,arrival_offset=0,workload=control"
    code:
      - fixed(5, control)
