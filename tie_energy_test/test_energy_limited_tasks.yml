taskset:
  - name: task_high_priority
    iat: 100
    runtime: 50
    deadline: 100
    params: "period=100,wcet=50,arrival_offset=0,workload=bzip2"
    code:
      - fixed(50, bzip2)

  - name: task_mid_priority
    iat: 100
    runtime: 30
    deadline: 100
    params: "period=100,wcet=30,arrival_offset=0,workload=hash"
    code:
      - fixed(30, hash)

  - name: task_low_priority
    iat: 100
    runtime: 20
    deadline: 100
    params: "period=100,wcet=20,arrival_offset=0,workload=control"
    code:
      - fixed(20, control)
