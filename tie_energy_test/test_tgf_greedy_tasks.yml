taskset:
  # Task1: 最高优先级 (周期最短)，高能耗
  - name: task1_high_priority
    iat: 100
    runtime: 50
    deadline: 100
    params: "period=100,wcet=50,arrival_offset=0,workload=bzip2"
    code:
      - fixed(50, bzip2)

  # Task2: 次高优先级，高能耗
  - name: task2_high_priority
    iat: 100
    runtime: 50
    deadline: 100
    params: "period=100,wcet=50,arrival_offset=0,workload=bzip2"
    code:
      - fixed(50, bzip2)

  # Task3: 中优先级，中能耗
  - name: task3_mid_priority
    iat: 150
    runtime: 10
    deadline: 150
    params: "period=150,wcet=10,arrival_offset=0,workload=hash"
    code:
      - fixed(10, hash)

  # Task4: 低优先级，低能耗
  - name: task4_low_priority
    iat: 200
    runtime: 5
    deadline: 200
    params: "period=200,wcet=5,arrival_offset=0,workload=control"
    code:
      - fixed(5, control)

  # Task5: 最低优先级，极低能耗
  - name: task5_low_priority
    iat: 200
    runtime: 2
    deadline: 200
    params: "period=200,wcet=2,arrival_offset=0,workload=control"
    code:
      - fixed(2, control)
