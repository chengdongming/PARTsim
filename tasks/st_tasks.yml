# ST调度器测试任务集
taskset:
  - name: Task_High
    iat: 100
    runtime: 10
    deadline: 100
    params: "period=100,wcet=10,arrival_offset=0,workload=bzip2"
    code:
      - fixed(10, bzip2)

  - name: Task_Mid
    iat: 200
    runtime: 15
    deadline: 200
    params: "period=200,wcet=15,arrival_offset=0,workload=hash"
    code:
      - fixed(15, hash)

  - name: Task_Low
    iat: 300
    runtime: 20
    deadline: 300
    params: "period=300,wcet=20,arrival_offset=0,workload=control"
    code:
      - fixed(20, control)
